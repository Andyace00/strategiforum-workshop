name: keepalive

on:
  schedule:
    - cron: "*/10 * * * *"   # hvert 10. min
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: |
          curl -sSf https://strategiforum-workshop.onrender.com/healthz || true
